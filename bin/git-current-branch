#!/usr/bin/env bash
#
# Git extension that prints the name of the current branch.

ref=$(git symbolic-ref HEAD 2>/dev/null) \
  || ref=$(git rev-parse --short HEAD 2>/dev/null) \
  || return
echo "${ref#refs/heads/}"
