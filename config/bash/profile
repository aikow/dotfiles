if [[ -n "${BASH_VERSION}" ]]; then
	# include .bashrc if it exists
	if [[ -f "${HOME}/.bashrc" ]]; then
		source "${HOME}/.bashrc"
	fi
fi

[[ -f "${HOME}/.local/config/bash_profile" ]] || source "${HOME}/.local/config/bash_profile"
